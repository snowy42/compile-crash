{
  "name": "compile-crash",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A keyboard-powered programming roguelite with progressive onboarding.",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "node scripts/server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
